# Copier en `.env` (non committé) pour personnaliser. Valeurs DEV LOCAL uniquement.
# Le `.env` est lu par docker-compose ; l'API lit les mêmes variables (ou ConnectionStrings__Default).
POSTGRES_USER=kings
POSTGRES_DB=kingscloud
POSTGRES_PASSWORD=kings_local_dev
POSTGRES_PORT=5432
